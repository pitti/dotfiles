After checking out, use the following commands to checkout the vundle submodule:

 * git submodule init
 * git submodule update


Then, use :BundleInstall to install all plugin bundles specified in the vimrc.
