#!/bin/bash

# Notify instant with terminal bell and OSD, if available.
# Usage: <cmd-that-takes-long> ; notify
# If the command finishes, 


# Issue bell
echo -e "\a"


# notify via OSD
[ -x $(which notify-send) ] && notify-send "Process finished" "Finished execution of your command." --icon=dialog-information
